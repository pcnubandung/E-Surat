{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "caddy run --config /app/Caddyfile --adapter caddyfile",
    "restartPolicyType": "ON_FAILURE"
  }
}
