{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo '=== dist contents ===' && ls -la /app/dist && ls -la /app/dist/assets && caddy run --config /app/Caddyfile",
    "restartPolicyType": "ON_FAILURE"
  }
}
